{
  "name": "flight-search",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "repository": { "type": "git", "url": "git+https://github.com/freeappstore-online/flight-search.git" },
  "scripts": {
    "dev": "pnpm --filter @flight-search/web dev",
    "build": "pnpm --filter @flight-search/web build",
    "preview": "pnpm --filter @flight-search/web preview",
    "typecheck": "pnpm --filter @flight-search/web exec tsc -b",
    "test": "pnpm --filter @flight-search/web exec tsc -b"
  }
}
